services:
  - type: web
    name: cafe-rodante-nfc
    env: python
    buildCommand: "./build.sh"
    startCommand: "python manage.py migrate && gunicorn cafe_rodante.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "*"
